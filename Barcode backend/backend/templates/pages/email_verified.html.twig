<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Email vérifié</title>
  <style>
    :root {
      --bg1: #0b1220;
      --bg2: #0a2a3a;
      --card: rgba(255,255,255,.08);
      --card2: rgba(255,255,255,.06);
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.72);
      --accent: #4ade80;
      --accent2: #22c55e;
      --border: rgba(255,255,255,.14);
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      color: var(--text);
      background:
        radial-gradient(1000px 600px at 20% 15%, rgba(34,197,94,.25), transparent 60%),
        radial-gradient(900px 600px at 85% 20%, rgba(59,130,246,.20), transparent 60%),
        linear-gradient(180deg, var(--bg1), var(--bg2));
      display: grid;
      place-items: center;
      padding: 24px;
    }
    .brand{
        display:flex;
        align-items:center;
        gap:12px;
        margin-bottom:16px;
    }
    .brand-logo{
        width:44px;
        height:44px;
        border-radius:16px;
        padding:6px;
        background: rgba(255,255,255,.08);
        border: 1px solid var(--border);
    }
    .brand-name{
        font-weight: 850;
        letter-spacing: .3px;
        font-size: 16px;
        color: rgba(255,255,255,.92);
        line-height: 1.1;
    }
    .brand-tagline{
        margin-top: 3px;
        font-size: 12px;
        color: var(--muted);
        line-height: 1.2;
    }
    .card {
      width: min(560px, 100%);
      background: linear-gradient(180deg, var(--card), var(--card2));
      border: 1px solid var(--border);
      border-radius: 18px;
      padding: 24px;
      box-shadow: 0 20px 70px rgba(0,0,0,.35);
      backdrop-filter: blur(10px);
    }
    .row { display: flex; gap: 14px; align-items: center; }
    .badge {
      width: 52px; height: 52px;
      border-radius: 14px;
      background: rgba(74,222,128,.16);
      border: 1px solid rgba(74,222,128,.35);
      display: grid;
      place-items: center;
      flex: 0 0 auto;
    }
    h1 {
      margin: 0;
      font-size: 22px;
      letter-spacing: .2px;
    }
    p { margin: 10px 0 0; color: var(--muted); line-height: 1.5; }
    .actions { display: flex; gap: 10px; flex-wrap: wrap; margin-top: 18px; }
    .btn {
      appearance: none;
      border: 1px solid rgba(255,255,255,.18);
      background: rgba(255,255,255,.08);
      color: var(--text);
      padding: 10px 14px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      cursor: pointer;
      transition: transform .08s ease, background .2s ease, border-color .2s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }
    .btn:hover { background: rgba(255,255,255,.12); border-color: rgba(255,255,255,.26); }
    .btn:active { transform: translateY(1px); }
    .btn.primary {
      background: linear-gradient(180deg, rgba(34,197,94,.95), rgba(22,163,74,.95));
      border-color: rgba(34,197,94,.6);
      color: #04130a;
    }
    .btn.primary:hover { filter: brightness(1.02); }
    .hint {
      margin-top: 14px;
      padding: 12px 14px;
      border-radius: 12px;
      border: 1px dashed rgba(255,255,255,.22);
      color: rgba(255,255,255,.78);
      background: rgba(0,0,0,.15);
      font-size: 13px;
    }
    code {
      color: rgba(255,255,255,.92);
      background: rgba(0,0,0,.22);
      padding: 2px 6px;
      border-radius: 8px;
      border: 1px solid rgba(255,255,255,.12);
    }
    .footer { margin-top: 14px; font-size: 12px; color: rgba(255,255,255,.55); }
    svg { display: block; }
  </style>
</head>
<body>
  <main class="card" role="main" aria-label="Email vérifié">

    <div class="brand">
        <img class="brand-logo" src="/assets/branding/frigozen_icon.png" alt="FrigoZen" />
        <div class="brand-text">
            <div class="brand-name">FrigoZen</div>
            <div class="brand-tagline">Ton frigo, plus zen.</div>
        </div>
    </div>

    <div class="row">
      <div class="badge" aria-hidden="true">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
          <path d="M20 6L9 17l-5-5" stroke="rgba(74,222,128,.95)" stroke-width="2.8" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
      <div>
        <h1>Email vérifié ✅</h1>
        <p>Merci ! Ton adresse email a bien été confirmée. Tu peux retourner dans l’application.</p>
      </div>
    </div>

    <div class="actions">
      <a class="btn primary" id="openApp" href="{{ deepLink }}">Ouvrir l’app</a>
      <a class="btn" href="{{ fallbackUrl }}">Retour au site</a>
    </div>

    <div class="hint" id="hint" style="display:none;">
      Si l’app ne s’ouvre pas, copie/colle ce lien dans ton navigateur mobile : <code id="dl"></code>
    </div>

    <div class="footer">
      Ce lien de vérification est valable jusqu’à expiration (sécurité anti-fraude).
    </div>
  </main>

  <script>
    (function () {
      const deepLink = "{{ deepLink|e('js') }}";
      const isMobileServer = {{ isMobile ? 'true' : 'false' }};
      const ua = navigator.userAgent || "";
      const isMobileClient = /Android|iPhone|iPad|iPod/i.test(ua);
      const isMobile = isMobileServer || isMobileClient;

      // Affiche le fallback au cas où
      const hint = document.getElementById("hint");
      const dl = document.getElementById("dl");
      if (dl) dl.textContent = deepLink;

      // Tentative auto d’ouverture sur mobile (sans forcer sur PC)
      if (isMobile) {
        // petit délai pour laisser la page se peindre
        setTimeout(() => {
          window.location.href = deepLink;
          // si l’ouverture échoue, au moins l’utilisateur a le bouton + hint
          setTimeout(() => { if (hint) hint.style.display = "block"; }, 900);
        }, 350);
      }
    })();
  </script>
</body>
</html>
